# Rule: language

Set your preferred language for AI responses here.

Example: "Respond in English" or "Отвечай на русском языке".
Technical terms can stay in English if commonly used (API, endpoint, commit, PR, deploy, etc.).

Code comments — match the project language (check existing code).
Commit messages — match the project convention (check `git log`).
