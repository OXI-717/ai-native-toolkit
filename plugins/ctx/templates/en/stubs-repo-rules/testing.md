# Tests and build

TODO: fill in commands for running tests, linters, and builds.

Example:
- Tests: `make test` or `pytest`
- Linter: `ruff check .`
- Formatter: `ruff format .`
- Build: `make build`
