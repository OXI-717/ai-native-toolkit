# Границы — что не трогать и почему

TODO: заполни файлы/папки/системы, которые не нужно изменять, и почему.

Пример:
- `legacy/` — старый код, не поддерживается, миграция в процессе
- `migrations/0001_*` — финализированные миграции, не изменять
- `.env.prod` — прод-кред-файлы только через секрет-менеджер
